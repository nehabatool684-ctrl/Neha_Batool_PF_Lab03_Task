# Algorithm to Find Even and Odd Numbers

- Start
- Input a number `N`.
- Divide the input number `N` by 2 and find the remainder.
- If the remainder is equals to `0`, then `N` is an even number.
- Otherwise, `N` is an odd number.
- Display whether `N` is even or odd.
- Stop
